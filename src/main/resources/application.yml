server:
  servlet:
    path: /
  tomcat:
    max-threads: 800
    uri-encoding: UTF-8
  port: 28101

spring:
    datasource:
        name: air
        url: jdbc:mysql://localhost:3306/air_server
        username: root
        password: wangshihao
        # 使用druid数据源
        type: com.alibaba.druid.pool.DruidDataSource
        driver-class-name: com.mysql.jdbc.Driver

mybatis:
    mapperLocations: classpath*:mapper/*.xml
    typeAliasesPackage: com.ten.air.tcp.entity